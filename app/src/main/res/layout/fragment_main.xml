<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <Spinner
        android:id="@+id/meat_type"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Spinner
        android:id="@+id/meat_doneness"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/meat_type"
        android:spinnerMode="dropdown" />


    <TextView
        android:id="@+id/start_cooking_button"
        android:layout_width="@dimen/menu_icons"
        android:layout_height="@dimen/menu_icons"
        android:layout_centerInParent="true"
        android:background="@drawable/rounded_tv"
        android:elevation="@dimen/elevation"
        android:gravity="center"
        android:text="@string/start_cooking" />
</RelativeLayout>
